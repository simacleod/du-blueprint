# Renderer App Icons

The renderer intentionally ships without bundle icons. The Tauri configuration sets the
`bundle.icons` array to empty so builds do not expect any image assets. If you want
custom icons, add them here and update `tauri.conf.json` accordingly.
